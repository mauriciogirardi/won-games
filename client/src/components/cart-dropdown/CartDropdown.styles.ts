import styled from 'styled-components'
import { CardListContainer } from '../card-list/CardList.styles'

export const CartDropdownContainer = styled.div`
  ${CardListContainer} {
    width: 56rem;
  }
`
